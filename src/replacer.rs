use image::{Rgb, ImageBuffer};

// Convert to an ImageBuffer from the image crate to allow text writing in different fonts
fn mat_to_image_buffer() -> Result<()> {
    unimplemented!()
}

// 
fn write_text() -> Result<()> {
    unimplemented!()
}

fn replace_text_region() -> Result<()> {
    unimplemented!()
}